# raw2ready Environment Configuration
# ---
# Set the directory where all output files (reports, exports, charts, logs)
# will be saved. If not set, defaults to ~/.raw2ready

# RAW2READY_OUTPUT_DIR=/path/to/your/output/directory
